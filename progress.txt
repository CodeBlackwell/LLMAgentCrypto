# Progress Log

## Learnings
(Patterns discovered during implementation)
- The trading_lab.db file persists between runs and doesn't auto-migrate. When adding new columns, delete the db file to recreate schema.
- Tests use in-memory SQLite via fixtures, but the app may init the real db first. The `override_db` fixture patches the database module.
- No mypy in project - uses ruff for linting. Import verification via python -c is sufficient for typecheck.

---

## Iteration 1 - US-001: Add progress fields to database schema
- What was implemented:
  - Added 5 new columns to BacktestRun model: progress_percent, progress_message, current_date, total_days, processed_days
  - Updated to_dict() method to include all progress fields plus error_message
- Files changed:
  - trading_lab/storage/models.py
- Learnings for future iterations:
  - SQLite database file (trading_lab.db) must be deleted when schema changes since there's no migration tool
  - All 227 tests pass after schema update
---

## Iteration 2 - US-002: Add repository method for progress updates
- What was implemented:
  - Added `update_progress()` method to `BacktestRepository` class
  - Method updates progress_percent (always), progress_message, current_date, processed_days (only if non-None/non-empty)
  - Method returns updated BacktestRun or None if not found
- Files changed:
  - trading_lab/storage/repository.py
- Learnings for future iterations:
  - Use .venv: `source .venv/bin/activate` before running pytest
  - Tests are located at trading_lab/tests/ not tests/
  - Follow existing patterns in repository (like update_status and update_results methods)
---

## Iteration 3 - US-003: Add progress fields to API schema
- What was implemented:
  - Added 6 progress fields to BacktestResponse in api/schemas.py
  - Fields: progress_percent, progress_message, current_date, total_days, processed_days, error_message
  - GET /backtests/{id} endpoint automatically returns these fields via to_dict() conversion
- Files changed:
  - trading_lab/api/schemas.py
- Learnings for future iterations:
  - The API routes use `BacktestResponse(**run.to_dict())` pattern, so adding fields to both the schema and model's to_dict() is sufficient
  - All 227 tests continue to pass
---

## Iteration 4 - US-004: Create ProgressTracker module
- What was implemented:
  - Created new `trading_lab/backtest/progress.py` module
  - ProgressTracker class with __init__, update, set_phase, complete methods
  - Calculates progress percent based on days processed / total days
  - Throttles DB updates to max once per 500ms using time-based check
  - Persists progress via BacktestRepository.update_progress()
- Files changed:
  - trading_lab/backtest/progress.py (new file)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Used TYPE_CHECKING for repository import to avoid circular imports
  - Throttling uses time.time() * 1000 for millisecond precision
  - complete() always persists immediately (ignores throttle) to ensure final state is saved
---

## Iteration 5 - US-005: Integrate progress tracking into backtest engine
- What was implemented:
  - Added optional `progress_tracker` parameter to BacktestEngine.__init__()
  - Calculate total_days from date range at start of run
  - Set phase transitions: "Fetching data...", "Simulating trades...", "Processing results..."
  - Call progress_tracker.update() with end date after simulation completes
  - Set total_days in database when progress_tracker is provided
- Files changed:
  - trading_lab/backtest/engine.py
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Lumibot's run_backtest() is a high-level blocking call that runs the entire simulation internally
  - Per-day progress updates within simulation require strategy-level integration (see US-006)
  - Use TYPE_CHECKING pattern to import ProgressTracker without circular imports
  - All 227 tests continue to pass
---

## Iteration 6 - US-006: Wire progress tracker into BacktestRunner
- What was implemented:
  - Modified `BacktestRunner._run_backtest()` to create ProgressTracker instance
  - Initialize total_days in database before running backtest
  - Create fresh db session and ProgressTracker for engine use (within safe_signal_handling context)
  - Pass progress_tracker to BacktestEngine via engine_kwargs
  - Call progress_tracker.complete() on successful completion
- Files changed:
  - trading_lab/backtest/runner.py
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Need fresh db session within safe_signal_handling block for thread safety
  - Create ProgressTracker twice: once to init total_days, once for engine (different db sessions)
  - All 227 tests continue to pass
---

## Iteration 7 - US-007: Add SSE streaming endpoint
- What was implemented:
  - Added GET /backtests/{backtest_id}/stream endpoint in trading_lab/api/routes/backtests.py
  - StreamingResponse with media_type="text/event-stream" and Cache-Control/Connection headers
  - Emits event types: progress (progress updates), trades (new trades), complete (success), error (failure)
  - Polls database every 500ms (asyncio.sleep), emits only on changes (tracks last_progress_percent, last_trade_count)
  - Auto-closes stream when status is terminal (completed or failed)
  - Removed unused ErrorResponse import (pre-existing lint issue)
- Files changed:
  - trading_lab/api/routes/backtests.py
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Use AsyncGenerator[str, None] for SSE streaming generator functions
  - TradeRepository.list_for_backtest() returns trades for a given backtest_id
  - SSE format: "event: {type}\ndata: {json}\n\n"
  - All 227 tests continue to pass
---

## Iteration 8 - US-008: Add server-side validation for backtest creation
- What was implemented:
  - Added model_validator to BacktestRequest schema for date range validation:
    - End date must be after start date
    - Date range must be <= 730 days (2 years)
  - Changed initial_cash Field from ge=0 to ge=100
  - Added strategy registry validation in POST /backtests route (checks list_strategies())
  - Returns 422 with descriptive error messages for validation failures
  - Threshold and position_size (cash_at_risk) already had ge=0.0 le=1.0 constraints
- Files changed:
  - trading_lab/api/schemas.py (added model_validator, changed initial_cash constraint)
  - trading_lab/api/routes/backtests.py (added strategy existence check)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Pydantic v2 uses model_validator(mode="after") for cross-field validation
  - Field constraints (ge, le) are most efficient for single-field validation
  - Strategy validation requires registry access, better done at route level not schema
  - All 227 tests continue to pass
---

## Iteration 9 - US-009: Increase worker pool and optimize runner
- What was implemented:
  - Changed `max_workers=2` to `max_workers=4` in BacktestRunner.__init__() default parameter
  - This increases the maximum concurrent backtests from 2 to 4
- Files changed:
  - trading_lab/backtest/runner.py (line 55)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Simple configuration changes like this are straightforward
  - ThreadPoolExecutor max_workers=4 is reasonable for most systems
  - All 227 tests continue to pass
---

## Iteration 10 - US-010: Create data cache module
- What was implemented:
  - Created `trading_lab/backtest/cache.py` with DataCache class
  - get() method retrieves cached DataFrame by asset/exchange/date range, returns None if not found
  - set() method stores DataFrame as pickle file in .backtest_cache/ directory
  - clear() method removes all .pkl files from cache directory
  - Cache key uses MD5 hash of asset_exchange_startdate_enddate string
  - Added .backtest_cache/ to .gitignore
- Files changed:
  - trading_lab/backtest/cache.py (new file)
  - .gitignore (added .backtest_cache/)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - TYPE_CHECKING pattern used for DataFrame import to avoid pandas dependency issues
  - Pickle files with .pkl extension for serialization
  - Exception handling for corrupted cache files (auto-cleanup on read failure)
  - All 227 tests continue to pass
---

## Iteration 11 - US-011: Display error banner for failed backtests
- What was implemented:
  - Error banner for failed backtests was already implemented in BacktestDetail.jsx (lines 141-166)
  - Red background (`bg-red-50 border border-red-200`) with SVG error icon
  - Displays `backtest.error_message` in `<pre>` tag with monospace font
  - Banner appears prominently after header, at top of results section
  - Only shows when `backtest.status === 'failed'` AND `backtest.error_message` exists
- Files changed:
  - PRD.md (marked task complete - implementation already existed)
- Learnings for future iterations:
  - Check existing code before implementing - task may already be done
  - Frontend build with `npm run build` is sufficient to verify JSX compilation
  - ESLint 9.x requires eslint.config.js but project doesn't have one - use build instead
  - All 227 tests continue to pass
---

## Iteration 12 - US-012: Add loading spinner for BacktestDetail
- What was implemented:
  - Replaced simple "Loading backtest..." text with centered spinner animation
  - Spinner uses Tailwind CSS classes: animate-spin, rounded-full, border styling
  - Container uses flex centering with py-16 for vertical spacing
  - Primary color border with transparent/light sides for spinning effect
  - Text label below spinner says "Loading backtest..."
- Files changed:
  - trading_lab/web/src/pages/BacktestDetail.jsx (lines 73-80)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Tailwind spinner pattern: `animate-spin rounded-full h-12 w-12 border-4 border-X-200 border-t-X-600`
  - npm run build verifies JSX compilation successfully
  - All 227 tests continue to pass
---

## Iteration 13 - US-013: Add running state indicator with elapsed time
- What was implemented:
  - Added blue banner (`bg-blue-50 border border-blue-200`) when `isRunning` is true
  - Included spinning loader animation (smaller version: h-6 w-6, border-2)
  - Display "Processing historical data for {asset}..." dynamically with asset name
  - Added `useState` and `useEffect` for elapsed time tracking
  - `formatElapsedTime()` helper converts seconds to human-readable format (Xh Xm Xs)
  - Timer updates every second using `setInterval`, with cleanup on unmount
- Files changed:
  - trading_lab/web/src/pages/BacktestDetail.jsx (added imports, helper function, state, effect, and banner)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Use optional chaining (`backtest?.created_at`) in useEffect dependencies
  - Timer cleanup is critical to avoid memory leaks
  - API server runs on port 8847, frontend on port 3847
  - Known bug: signal_provider must be explicitly passed (None causes validation error)
  - Exchange names are case-sensitive (use "kraken" not "Kraken")
  - All 227 tests continue to pass
---

## Iteration 14 - US-014: Add progress bar display
- What was implemented:
  - Added progress bar section inside the running state indicator banner
  - Large bold percentage text (text-3xl font-bold) displaying progress_percent
  - "Day X/Y" counter using processed_days and total_days
  - Visual progress bar with animated width transition (transition-all duration-500 ease-out)
  - Blue color scheme consistent with running state indicator (bg-blue-200 bar, bg-blue-600 fill)
  - Conditional progress_message display below the progress bar
- Files changed:
  - trading_lab/web/src/pages/BacktestDetail.jsx (added progress bar section at lines 229-245)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Use nullish coalescing (?? 0) for progress values that may be null/undefined
  - Tailwind transition classes work well for smooth progress bar animations
  - Progress bar should be nested inside existing running state banner for visual cohesion
  - All 227 tests continue to pass
---

## Iteration 15 - US-015: Create SSE hook for real-time updates
- What was implemented:
  - Created `trading_lab/web/src/hooks/useBacktestStream.js` React hook
  - Hook signature: `useBacktestStream(backtestId, enabled)` returns `{ data, error, isConnected }`
  - Uses native EventSource API to connect to `/api/backtests/{id}/stream` endpoint
  - Handles SSE event types: progress, trades, complete, error
  - Implements exponential backoff reconnection: 1s, 2s, 4s, 8s, up to 30s max
  - Proper cleanup on unmount (clears timeout and closes EventSource)
  - Merges incoming data with previous state for incremental updates
- Files changed:
  - trading_lab/web/src/hooks/useBacktestStream.js (new file)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - EventSource.addEventListener() handles named events from SSE stream (progress, trades, etc.)
  - useCallback with ref-based state avoids stale closure issues in reconnection logic
  - hooks/ directory existed but was empty - first hook in the project
  - npm run build verifies JSX/JS compilation for frontend code
  - All 227 tests continue to pass
---

## Iteration 16 - US-016: Integrate SSE hook in BacktestDetail
- What was implemented:
  - Imported `useBacktestStream` hook in BacktestDetail.jsx
  - Added `isActiveStatus` check to enable stream only when status is running/pending
  - Merged stream data with query data (stream takes precedence for progress fields)
  - Added useEffect to update React Query cache when stream data arrives
  - Added separate polling query that is disabled when SSE is connected
  - Added connection status indicator with green dot (connected) and yellow pulsing dot (reconnecting)
- Files changed:
  - trading_lab/web/src/pages/BacktestDetail.jsx
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Use separate queries for initial load and polling to maintain clean separation
  - `queryClient.setQueryData()` allows manual cache updates from SSE stream
  - Connection status indicator uses Tailwind's `animate-pulse` for reconnecting state
  - Build still passes (npm run build) and all 227 tests continue to pass
---

## Iteration 17 - US-017: Add tooltips to form fields
- What was implemented:
  - Created inline Tooltip component in NewBacktest.jsx
  - Tooltip uses useState for visibility, shows on hover/focus
  - Added tooltip icons (? button) next to Initial Cash, Threshold, and Position Size labels
  - Dark gray-800 background with white text, positioned to the right of the icon
  - Includes arrow/caret pointing to the icon using rotated div
- Files changed:
  - trading_lab/web/src/pages/NewBacktest.jsx (added Tooltip component and 3 tooltip instances)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Inline components work well for simple UI elements like tooltips
  - onMouseEnter/onMouseLeave plus onFocus/onBlur covers both mouse and keyboard accessibility
  - z-10 ensures tooltip appears above other elements
  - npm run build and all 227 tests continue to pass
---

## Iteration 18 - US-018: Add client-side form validation
- What was implemented:
  - Added validationErrors state to track field-level errors
  - Created validateField() function with switch statement for all validated fields
  - Added validateAllFields() to validate all fields at once (for submit)
  - Added handleBlur() function to validate on blur with cross-field date validation
  - Added hasValidationErrors computed value for submit button state
  - Added onBlur handlers to strategy_name, start_date, end_date, initial_cash, threshold, cash_at_risk
  - Added inline error messages (<p className="mt-1 text-sm text-red-600">) below each validated field
  - Added border-red-500 class to inputs with errors
  - Disabled submit button when hasValidationErrors is true (with opacity-50 styling)
- Files changed:
  - trading_lab/web/src/pages/NewBacktest.jsx (added validation state, functions, and UI)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Date validation requires cross-field checks - when blurring start_date, also revalidate end_date
  - Use formData parameter in validateField to get current form state for cross-field validation
  - Tailwind border-red-500 works well for input error styling
  - All 227 tests continue to pass
---

## Iteration 19 - US-019: Add estimated duration display
- What was implemented:
  - Added `calculateTradingDays()` function to count weekdays between start and end dates
  - Added `formatDuration()` function to format seconds as "Xm Ys" string
  - Added blue informational banner showing "Estimated duration: Xm Ys (N trading days)"
  - Banner only shows when tradingDays > 0 (valid date range)
  - Duration updates dynamically as dates change (recalculated on every render)
  - Uses ~2 seconds per trading day estimation
- Files changed:
  - trading_lab/web/src/pages/NewBacktest.jsx (added functions and banner section)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Trading day calculation excludes weekends (Saturday=0, Sunday=6)
  - Tailwind blue-50/blue-200 color scheme works well for informational banners
  - Clock icon SVG adds visual clarity to the duration estimate
  - All 227 tests continue to pass
---

## Iteration 20 - US-020: Add date picker presets
- What was implemented:
  - Added `formatDateString()` helper to convert Date to YYYY-MM-DD format
  - Added `applyDatePreset()` function with switch for 4 preset options
  - Date inputs were already type="date" (existed from previous implementation)
  - Added preset buttons section with "Quick select:" label
  - Buttons: "Last 3 months", "Last 6 months", "YTD", "Last year"
  - Each preset calculates start date relative to today and sets end date to today
  - Clears validation errors when preset is applied
  - Styled as rounded pill buttons with hover state
- Files changed:
  - trading_lab/web/src/pages/NewBacktest.jsx (added preset functions and UI)
  - PRD.md (marked task complete)
- Learnings for future iterations:
  - Date.toISOString().split('T')[0] is a simple way to get YYYY-MM-DD format
  - setMonth/setFullYear on Date object handles month overflow correctly
  - YTD uses template literal with current year: `${today.getFullYear()}-01-01`
  - All 227 tests continue to pass
---
